                 <% book_comments.each do |book_comment| %></br>
                   <%= attachment_image_tag(book_comment.user, :profile_image,fallback: "no-image-icon.jpg", size:'40x40') %>
                   <%= book_comment.comment %>
                   <%= link_to  user_path(book_comment.user) do %>
                   <% if book_comment.user == current_user %>
                   <%= link_to 'Destroy', book_book_comment_path(book_comment.book, book_comment), method: :delete, remote: true, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger destroy_book_#{book_comment.book.id}"%></br>
                   <% end %>
                   <%= book_comment.user.name %>
                   <% end %>
                 <% end %>